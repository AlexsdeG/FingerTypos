# Sound Assets

Place your audio files here.
- `click.mp3` or `click.wav` (Success sound, typing)
- `error.mp3` or `error.wav` (Error sound, typing)
- `applause.mp3` or `applause.wav` (Applause sound, level complete)

These files are referenced in `src/utils/sound.ts`.
